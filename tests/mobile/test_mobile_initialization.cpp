#include "MobileManager.h"

void test_mobile_initialization() {
    MobileManager manager;
    manager.initialize();
    // Vérifiez que l'initialisation se passe comme prévu
}

int main() {
    test_mobile_initialization();
    return 0;
}
