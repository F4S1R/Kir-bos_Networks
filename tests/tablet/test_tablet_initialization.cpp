#include "TabletManager.h"

void test_tablet_initialization() {
    TabletManager manager;
    manager.initialize();
    // Vérifier que l'initialisation s'est bien déroulée
}

int main() {
    test_tablet_initialization();
    return 0;
}
