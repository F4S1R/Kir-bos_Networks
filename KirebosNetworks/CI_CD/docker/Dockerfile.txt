# Base image
FROM ubuntu:latest

# Install dependencies
RUN apt-get update && apt-get install -y \
    build-essential \
    qt5-default \
    libssl-dev \
    python3 \
    python3-pip \
    docker

# Copy project files
COPY . /app
WORKDIR /app

# Run initial tests
CMD ["sh", "CI_CD/scripts/run_tests.sh"]
